////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////                                 NULL pointer
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include<stdio.h>

#define NULL (void *) 0 

int main()
{
    int *ptr = NULL ;     // we can also see this like : int *ptr = 0 ;


    // The syntax : int *ptr = NULL ; After perprocessing the above syntax it converted into : int *ptr = (void *) 0 ;
    int *ptr = (void *) 0 ;

    return 0 ;
}
